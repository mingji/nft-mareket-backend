export enum FeeMethod {
    protocolFee = 0,
    splitFee = 1
}

export enum Side {
    buy = 0,
    sell = 1
}

export enum SaleKind {
    fixedPrice = 0,
    dutchAuction = 1
}

export enum HowToCall {
    call = 0,
    delegateCall = 1
}